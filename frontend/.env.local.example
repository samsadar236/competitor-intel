# Base URL of the FastAPI backend. Locally this is http://localhost:8000
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# Set to "true" to render from bundled mock data without a running backend.
NEXT_PUBLIC_USE_MOCK=true
